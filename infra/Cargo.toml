[package]
name = "infra"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
logger = { path = "../logger" }
infra_config = { path = "../infra_config" }

tracing = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }