[package]
name = "monitor"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
logger = { path = "../logger" }

tracing = { workspace = true }
sha2 = { workspace = true }